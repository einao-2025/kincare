{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "corepack enable && corepack prepare pnpm@9.7.0 --activate && pnpm install --frozen-lockfile && pnpm --filter @kincare/db build && pnpm --filter @kincare/web... build",
  "installCommand": "echo 'install handled in buildCommand'",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- apps/web packages"
}
